import * as React from "react";
import ReactDOM from "react-dom";

const Options = () => {
  return <>{/* todo: add options */}</>;
};

ReactDOM.render(
  <React.StrictMode>
    <Options />
  </React.StrictMode>,
  document.getElementById("root")
);
