import * as React from "react";
import ReactDOM from "react-dom";

const Popup = () => {
  return <>{/* todo: add setting chatgpt api key */}</>;
};

ReactDOM.render(
  <React.StrictMode>
    <Popup />
  </React.StrictMode>,
  document.getElementById("root")
);
